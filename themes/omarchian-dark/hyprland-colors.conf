# Omarchian Dark — default theme colors, referenced by hyprland.conf.
# Swap the symlink at ~/.config/omarchian/current/theme to switch themes;
# add new ones under themes/<name>/ following this same file layout.

$primary  = rgba(8caaeeee)
$surface  = rgba(414559aa)
$base     = rgba(303446ee)
$text     = rgba(c6d0f5ff)
